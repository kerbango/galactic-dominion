{
  "name": "Unit",
  "type": "object",
  "properties": {
    "unit_type": {
      "type": "string",
      "title": "Unit Type",
      "description": "The unit type id (matches a unit in the shared units dataset)."
    },
    "owned_count": {
      "type": "number",
      "title": "Owned Count",
      "description": "Number of completed ships of this type the player owns.",
      "default": 0,
      "minimum": 0
    },
    "construction_start_date": {
      "type": "string",
      "format": "date-time",
      "title": "Construction Start Date",
      "description": "ISO timestamp when the currently active build started. Null when no ship is actively building."
    },
    "construction_turns": {
      "type": "number",
      "title": "Construction Turns",
      "description": "Snapshot of the active ship's buildTurns at start. Completion = start_date + construction_turns * BASE_TURN_SECONDS.",
      "default": 1,
      "minimum": 1
    },
    "construction_queue": {
      "type": "number",
      "title": "Construction Queue",
      "description": "Number of additional ships of this type already paid for and waiting to be constructed after the active ship.",
      "default": 0,
      "minimum": 0
    },
    "upgrade_levels": {
      "type": "object",
      "title": "Upgrade Levels",
      "description": "Map of upgrade_id to purchased level for this unit type. Per-type upgrades apply to all owned ships of this type.",
      "default": {}
    }
  },
  "required": [
    "unit_type"
  ],
  "rls": {
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}